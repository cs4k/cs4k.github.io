import { NgZone } from '@angular/core';
import { Observable } from 'rxjs';
export declare class AutoClose {
    private _ngZone;
    private _document;
    constructor(_ngZone: NgZone, _document: any);
    install(autoClose: boolean | 'inside' | 'outside', close: () => void, closed$: Observable<any>, insideElements: HTMLElement[], ignoreElements?: HTMLElement[]): void;
}
