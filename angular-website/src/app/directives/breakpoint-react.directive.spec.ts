import { BreakpointReactDirective } from './breakpoint-react.directive';

describe('BreakpointReactDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakpointReactDirective();
    expect(directive).toBeTruthy();
  });
});
